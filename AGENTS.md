# AGENTS.md

このファイルの指示はリポジトリ全体に適用します。

## プロジェクト

fukidashi は、YouTube / Twitch 向けコメント CSS ジェネレーターです。

- React + TypeScript
- Vite
- React Router
- Tailwind CSS
- Vitest

## デザインルール

UI の作成・変更・レビューを行う前に、必ずルートの `DESIGN.md` を確認し、その方針に従ってください。

- 既存のデザイントークン、パターン、`src/components/ui` の共通コンポーネントを優先して再利用する
- 新しい色、フォント、角丸、独自 UI パターンを追加する場合は、必要性を確認して `DESIGN.md` も更新する
- デスクトップとモバイルの両方を考慮する
- hover だけに依存せず、キーボード操作、フォーカス表示、適切なラベルと ARIA 属性を維持する
- 既存 UI の意図的な例外は、依頼の範囲外で無理に統一しない
- ジェネレーターが出力するコメント用 CSS と、その出力を再現するプレビュー内部は `DESIGN.md` のスペーシングルールの対象外とする。出力を囲むアプリ UI には適用する
- アプリ UI の色は `DESIGN.md` のセマンティックトークンを使い、`/60` などの透明度指定や未定義の色を追加しない。追加が必要な場合は先に相談する。ただし、モーダル背景など透明であること自体に意味がある表現は除く
- レスポンシブ指定はデスクトップを基本とし、`max-lg:*` だけを使用する。`sm:*`、`md:*`、`lg:*`、`max-md:*` など別のブレークポイントを追加しない
- 外形が見える操作要素は最低 `44 × 44px` を目安にしつつ、何も表示されていない透明な領域をクリック可能にしない。詳細は `DESIGN.md` のコンポーネントルールに従う
- スペーシング値 `1` は、`2` の配下でさらに一段小さい余白が必要な場合に限って使用できる

## 実装ルール

- TypeScript の型安全性を保ち、不要な型アサーションを避ける
- 機能単位のコードは `src/features`、共通 UI は `src/components/ui`、アプリ共通要素は `src/app` に配置する
- スタイルは既存方針に合わせて Tailwind CSS を使用する
- 既存の状態管理とコンポーネント境界を尊重し、依頼と無関係なリファクタリングを混ぜない
- YouTube / Twitch やテンプレートごとの表示条件を変更する場合は、他の組み合わせへの影響も確認する
- 新しい依存パッケージは、既存の仕組みで実現できない場合にのみ追加する

## 検証

変更内容に応じて、次のコマンドを実行してください。

```bash
npm run lint
npm test
npm run build
```

UI の変更では、主要なデスクトップ幅と 320px 以上のモバイル幅を確認してください。
