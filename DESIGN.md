# fukidashi Design Guide

このドキュメントは、fukidashi の UI を新規作成・変更するときの判断基準です。既存画面との一貫性、わかりやすさ、配信者が迷わず CSS を作れることを優先します。

## 適用範囲

このガイドは、設定画面、ヘッダー、フッター、モーダルなど、fukidashi のアプリ UI に適用します。

ジェネレーターが出力するコメント用 CSS は対象外です。`src/features/generator/generateCss.ts`、`src/features/generator/templates`、および出力結果を再現するためのプレビュー内部では、Tailwind の `16 → 12 → 8 → 4 → 2` のスペーシング階層に従う必要はありません。YouTube、Twitch、OBS の仕様と、各コメントテンプレートの見た目を優先してください。

ただし、プレビューの外枠、設定コントロール、生成 CSS を表示するモーダルなど、出力内容を囲むアプリ UI にはこのガイドを適用します。

## 1. デザインの方向性

fukidashi は、やわらかく親しみやすい印象と、設定ツールとしての明快さを両立します。

- 余白を広めに取り、情報を詰め込みすぎない
- 深いチャコールを基調にし、サーフェスの明度差と明るい文字色で情報の階層を伝える
- 大きな角丸とピル型の操作要素で、吹き出しらしいやわらかさを表現する
- 装飾よりも、現在の状態と次に行う操作のわかりやすさを優先する
- 日本語と英語が混在しても、文字の役割と階層が自然に伝わるようにする

## 2. カラー

アプリ UI はダークテーマを標準とし、色名ではなく役割に基づくセマンティックトークンを使用します。新しい色を追加する前に、既存トークンで目的を満たせないか確認してください。

| トークン         | 色                 | 使用例                                     |
| ---------------- | ------------------ | ------------------------------------------ |
| Primary          | `#5997F2`          | 選択状態、スイッチ ON、主要操作、フォーカス |
| Secondary        | `#FFB5D5`          | 補助アクセント、完了状態、装飾アイコン     |
| Secondary Strong | `#FFA3CB`          | 主要操作に添える小さなアクセントアイコン   |
| Background       | `#1A1B20`          | ページ背景、最も低い階層                   |
| Surface          | `#27292C`          | ヘッダー、設定パネル、モーダル             |
| Surface Raised   | `#383A3D`          | 入力欄、セグメント、サーフェス内の操作要素 |
| Surface Hover    | `#44464A`          | Surface 系操作要素の hover                 |
| Action Surface   | `#3B3D3E`          | Create ボタンの背景                        |
| Foreground       | `#FFFFFF`          | 見出し、本文、主要アイコン                 |
| Foreground Muted | `#A9A9AD`          | 補足、未選択ラベル、通知                   |
| Border           | `#5B5D63`          | 枠線、OFF、区切り                          |
| On Primary       | `#1A1B20`          | Primary 上の文字とアイコン                 |
| Overlay          | `rgb(0 0 0 / 70%)` | モーダル背景                               |

実装ではそれぞれ `--color-primary`、`--color-secondary`、`--color-secondary-strong`、`--color-background`、`--color-surface`、`--color-surface-raised`、`--color-surface-hover`、`--color-action-surface`、`--color-foreground`、`--color-foreground-muted`、`--color-border`、`--color-on-primary`、`--color-overlay` を使用します。Tailwind CSS では `bg-primary`、`bg-surface`、`text-foreground`、`border-border` など、役割が読み取れるクラスを指定します。

Primary は操作の選択と進行を示す色、Secondary は補助的な強調色として使い分けます。透明度指定によって独自の色段階を作らず、Surface 系の hover には Surface Hover を使います。画像・アニメーションに対する一時的な opacity や brightness の変化と、透明であること自体に意味がある Overlay は例外です。コメントのカラープリセットは生成対象のテーマ色であり、アプリ UI の Primary / Secondary とは分けて扱います。

### コントラスト

- 通常の本文は原則として Foreground を使用する
- Foreground Muted は Background、Surface、Surface Raised のいずれとの組み合わせでも、通常サイズの文字で WCAG 2.1 AA を満たす範囲で使用する
- Border は意味を持つ文字に使わず、枠線、区切り、OFF など主要情報ではない箇所に限定する
- Primary 上の文字とアイコンには On Primary を使用する。Foreground は Primary 上の通常サイズの文字には使わない
- 色だけに状態を依存させず、枠線、チェック、ラベルなどを併用する
- 新しい組み合わせは WCAG 2.1 AA のコントラストを目安に確認する

## 3. タイポグラフィ

### フォント

- 日本語と本文: `Noto Sans JP`, sans-serif
- 英字のロゴ、短い見出し、操作ラベル: `Poppins`, sans-serif
- コード・生成 CSS: ブラウザ標準の等幅フォント、または明示した monospace フォント

### 基本サイズ

| 用途               | 目安                        |
| ------------------ | --------------------------- |
| ページタイトル     | `24–36px` / Bold            |
| セクションタイトル | `16–24px` / Semibold–Bold   |
| 本文               | `14–16px` / Regular–Medium  |
| 操作ラベル         | `12–16px` / Medium–Semibold |
| 補足テキスト       | `12px` / Regular–Medium     |

本文は読みやすい行間を確保し、長文では `1.7–2.0` を目安にします。英字ラベルの大文字・小文字は既存 UI の表記に合わせます。

## 4. 余白とレイアウト

### スペーシングの基本単位

`padding`、`margin`、`gap` は、Tailwind CSS のクラス名の数値を基準に指定します。ここで扱う `16`、`12`、`8`、`4`、`2` はピクセル値ではなく、`p-16` や `gap-8` のような Tailwind のスペーシング値です。

| 段階 | Padding | Gap      | CSS の実寸 |
| ---- | ------- | -------- | ---------- |
| `16` | `p-16`  | `gap-16` | `64px`     |
| `12` | `p-12`  | `gap-12` | `48px`     |
| `8`  | `p-8`   | `gap-8`  | `32px`     |
| `4`  | `p-4`   | `gap-4`  | `16px`     |
| `2`  | `p-2`   | `gap-2`  | `8px`      |

- 標準の段階は `16`、`12`、`8`、`4`、`2` とする
- `px-*`、`py-*`、`pt-*`、`mb-*` など方向を限定する場合も同じ段階から選ぶ
- `2` の配下でさらに一段小さい余白が必要な場合は、`1` を使用できる
- `1` は標準段階として単独で使わず、直上の `2` との階層関係が明確な箇所に限定する
- `p-6`、`gap-3`、`py-1.5` など、段階の間にある値は原則として新しく追加しない
- `p-[18px]` のような arbitrary value は、外部仕様に合わせる必要がある場合を除いて使わない

### 入れ子の階層

入れ子になった要素の余白は、外側より一段階小さい Tailwind のスペーシング値を使います。基本の階層は `16 → 12 → 8 → 4 → 2` とし、`2` の配下に限って `1` まで使用できます。

| 階層 | 用途の例                         | Tailwind の例   |
| ---- | -------------------------------- | --------------- |
| 1    | ページや大きな領域               | `p-16`          |
| 2    | 領域内のセクション間             | `gap-12`        |
| 3    | セクションやカードの内側         | `p-8` / `gap-8` |
| 4    | グループ内の要素                 | `p-4` / `gap-4` |
| 5    | ラベル、入力、アイコンのまとまり | `p-2` / `gap-2` |

たとえば、外側が `p-16` なら、その直下の要素間は `gap-12`、さらに内側のグループは `p-8` または `gap-8` を第一候補にします。`p-8` または `gap-8` の配下に入れ子を作る場合は `p-4` または `gap-4` にします。

- 同じ階層・同じ役割の要素には同じ値を使う
- 直接の親子関係では、内側の余白を外側と同じか一段階小さくする。理由なく内側を大きくしない
- 横方向と縦方向で値を変える場合も、それぞれ上記の段階から選ぶ
- 既存の中間値は、該当 UI を変更するときに近い標準値へ段階的に揃える

### ページレイアウト

- ページ左右の余白はデスクトップで `px-8`、モバイルで `px-4` を基本にする
- メインコンテンツは `max-w-7xl`、文章ページは `max-w-3xl` を基準にする
- ジェネレーターはデスクトップでプレビューと設定を横並びにし、設定パネルは `max-w-md` を基準にする
- モバイルでは一列にし、横スクロールを発生させない
- 固定高さや sticky を使う場合も、小さい画面で内容へ到達できることを確認する

レスポンシブの切り替えは `max-lg` のみを使用します。デスクトップのスタイルを基本とし、1024px 未満の表示を `max-lg:*` で上書きします。`sm:*`、`md:*`、`lg:*`、`max-md:*` など、別のブレークポイントは追加しません。

## 5. 角丸、枠線、影

- 大きなパネルとモーダル: `rounded-4xl`
- カードとコード入力欄: `rounded-2xl`
- 小さな入力欄と詳細行: `rounded-lg`
- ボタン、セグメント、スイッチ: `rounded-full`
- 選択カード: 通常 `2px`、選択時 `3px` の明確な枠線
- 影は控えめにし、階層は主に背景色、余白、枠線で表現する

同じ階層の要素では角丸の大きさを揃えます。新しい半径を場当たり的に増やさないでください。

## 6. コンポーネント

### ボタン

- 主要操作は Primary の背景、On Primary の文字、ピル型を基本にする
- Create ボタンは Action Surface の背景、Foreground の文字、Secondary Strong のアイコンを使用する
- 二次操作は Surface Raised の背景または Border の枠線とし、主要操作より目立たせない
- アイコンだけのボタンには必ずアクセス可能な名前を付ける
- 背景色や枠線などで外形が見える操作要素は、全体をクリック可能にし、最低 `44 × 44px` を目安にする
- クリック領域は見えている外形と一致させる。`44 × 44px` を満たすために、何も表示されていない透明な領域だけを広げない
- `Switch` や小さなカラースウォッチなど、見た目が `44px` 未満のコントロールは、見えている部分だけをクリック可能にする

### 選択コントロール

- 少数の排他的な選択肢には `SegmentedControl` を使う
- 見た目を比較して選ぶ選択肢には `RadioCardGroup` を使う
- ON / OFF の即時設定には `Switch` を使う
- 既存コンポーネントで表現できる場合は、新しい独自コントロールを作らない

### 入力欄

- 入力欄は Surface Raised の背景を基本とし、フォーカス時は Primary の枠線またはリングを表示する
- ラベルは入力欄とプログラム上で関連付ける
- エラーは原因と修正方法がわかる文章で表示し、色だけで示さない

### モーダル

- 共通の `Modal` コンポーネントを使用する
- タイトル、閉じる操作、Escape キー、背景クリック、フォーカス復元を維持する
- モバイルで上下が切れず、内部をスクロールできるようにする
- 破壊的でない通常操作に確認モーダルを重ねすぎない

## 7. インタラクションとモーション

- 色や不透明度の変化は `200–300ms` を基本にする
- テキストリンクと背景のないボタンは、hover で Secondary に変化させる
- Surface 系のカード、入力欄、ボタンは、hover で Surface Hover に変化させる
- Primary の操作は、hover で明度を少し上げる。選択済みコントロールの Primary は維持する
- アイコンだけのボタンとカラースウォッチは、hover で opacity を下げる
- hover のトランジションは `200ms` を基本とし、同じ役割の操作要素で揃える
- 完了メッセージなど、一時的なフィードバックは十分に読める時間表示する
- hover は補助的な表現とし、`max-lg:hover:*` で通常状態へ戻してタッチ操作でも意味が失われないようにする
- フォーカス表示を消さない。キーボード操作時には明確なリングを表示する
- `prefers-reduced-motion` が有効な場合は、大きな移動や拡大縮小を抑える
- アニメーションは状態変化の理解を助ける目的で使い、常時動かさない

## 8. レスポンシブ対応

ブレークポイントは `max-lg` に統一し、デスクトップと1024px未満の2段階で設計します。

### デスクトップ

- プレビューと設定パネルを横並びにする
- 設定が画面高を超える場合は、設定領域内でスクロールできるようにする
- プレビューは操作中も確認しやすい位置に保つ

### モバイル

- プレビュー、設定の順に一列で表示する
- ページ自体を自然に縦スクロールできるようにする
- 余白と見出しサイズを一段階小さくする
- hover 前提の見た目や操作を使わない
- 320px 幅でも主要操作とテキストが欠けないことを確認する

## 9. アクセシビリティ

- ページの言語は日本語として維持する
- 見出しレベルを飛ばさず、意味のある文書構造にする
- 装飾画像は空の `alt`、情報を持つ画像には内容が伝わる代替テキストを設定する
- フォームは `label`、`aria-labelledby`、`fieldset` などで関係を明示する
- モーダルや開閉 UI には `aria-modal`、`aria-expanded` など適切な状態を設定する
- キーボードだけで全機能を操作できるようにする
- 状態変更やコピー完了など、必要なフィードバックは支援技術にも伝わるようにする

## 10. 文言

- 短く、具体的で、次の操作が想像できる表現にする
- 同じ概念には同じ言葉を使う
- 日本語の説明文では専門用語を避け、必要な場合は最初に意味を説明する
- ボタンは可能な限り動作を表す言葉にする
- YouTube、Twitch など固有名詞の正式な表記を使う

## 11. 実装時の確認

UI を変更したら、最低限次を確認します。

- デスクトップとモバイルの両方でレイアウトが崩れていない
- 通常、hover、focus、選択、無効、エラーの各状態が判別できる
- キーボードだけで操作でき、フォーカス位置が見える
- ラベル、代替テキスト、ARIA 属性が適切である
- 既存の UI コンポーネントを再利用している
- `npm run lint`、`npm test`、`npm run build` が成功する

## 12. Do / Don't

### Do

- 既存の色、余白、角丸、コンポーネントを組み合わせる
- 重要な操作ほど視覚的な優先度を高くする
- 実際の日本語文言と長いコメントでレイアウトを確認する
- 状態変化を視覚とセマンティクスの両方で伝える

### Don't

- 目的のないグラデーション、強い影、過剰なアニメーションを追加しない
- 新しい色や似た役割のコンポーネントを安易に増やさない
- 色や hover だけで操作方法や状態を伝えない
- デスクトップだけを基準に固定幅・固定高を追加しない
- プレビュー用のテーマ色をアプリ全体の UI 色として流用しない
