    /* ----------------------------------------------------
      ©︎通行止め.ᐟ.ᐟ
    ---------------------------------------------------- */
    @import url("https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;700&family=Noto+Sans+SC:wght@400;700&family=Noto+Sans+TC:wght@400;700&family=Noto+Sans+KR:wght@400;700&family=Noto+Sans:wght@400;700&display=swap");
    @import url("https://plus-5min.github.io/live-chat-css/twitch/hide.css");

    :root {
      --font: "Noto Sans JP", sans-serif;

      --listener-name: {{listener-name}};
      --listener-name-bg: {{listener-name-bg}};
      --listener-comment: {{listener-comment}};
      --listener-comment-bg: {{listener-comment-bg}};
      --listener-comment-border: {{listener-comment-border}};
    }

    @keyframes popInLeft {
      0% {
        opacity: 0;
        transform: translate(-50%, -50%) scale(0);
      }
      100% {
        opacity: 1;
        transform: translate(0, 0) scale(1);
      }
    }

    @keyframes popInRight {
      0% {
        opacity: 0;
        transform: translate(50%, -50%) scale(0);
      }
      100% {
        opacity: 1;
        transform: translate(0, 0) scale(1);
      }
    }

    /* ----------------------------------------------------
      app settings
    ---------------------------------------------------- */
    html,
    body,
    #root,
    .chat-room,
    .chat-line__moderation,
    .chat-line__status,
    .chat-line__message {
      margin: 0 auto;
      background-color: transparent !important;
    }

    body,
    .chat-room,
    .chat-line__moderation,
    .chat-line__status,
    .chat-line__message,
    .chat-line__message * {
      font-family: var(--font) !important;
      font-size: 16px !important;
      font-style: normal !important;
      font-weight: 700 !important;
      line-height: 1.5 !important;
      letter-spacing: 0.5px !important;
    }

    body,
    .chat-room,
    .scrollable-area {
      overflow: hidden !important;
    }

    .chat-scrollable-area__message-container {
      padding-block-start: 0 !important;
    }

    /* ----------------------------------------------------
      text message
    ---------------------------------------------------- */
    .chat-line__message {
      --chat-name: var(--listener-name);
      --chat-name-bg: var(--listener-name-bg);
      --chat-comment: var(--listener-comment);
      --chat-comment-bg: var(--listener-comment-bg);
      --chat-comment-rendered-bg: {{listenerCommentBackground}};
      --chat-comment-border: var(--listener-comment-border);
      --chat-comment-border-style: {{listenerBorder}};

      position: relative;
      display: block !important;
      box-sizing: border-box;
      width: 100%;
      padding: 8px 16px !important;
      overflow: visible !important;
      animation: {{animationName}} 0.3s ease-out forwards;
    }

    .chat-line__no-background {
      display: grid !important;
      grid-gap: 6px;
      width: fit-content;
      {{autoMarginSide}}: auto;
    }

    .chat-line__username-container,
    .chat-line__username-container.chat-line__username-container--hoverable {
      display: flex !important;
      flex-direction: row-reverse;
      grid-gap: 8px;
      width: fit-content;
      margin-bottom: 6px;
      white-space: nowrap;
      text-overflow: ellipsis;
      {{autoMarginSide}}: auto;
    }

    .chat-line__username-container span {
      display: {{authorDisplay}};
    }

    .chat-line__username {
      display: {{authorDisplay}} !important;
      padding: 4px 12px;
      border-radius: 18px;
      background-color: var(--chat-name-bg) !important;
    }

    .chat-author__display-name {
      display: {{authorDisplay}} !important;
      width: fit-content;
      color: var(--chat-name) !important;
      font-weight: 700;
      text-wrap: wrap;
    }

    .chat-badge {
      display: {{authorDisplay}} !important;
      margin-bottom: 0 !important;
      vertical-align: bottom !important;
    }

    span[data-a-target="chat-line-message-body"] {
      position: relative;
      display: block !important;
      box-sizing: border-box;
      width: fit-content !important;
      max-width: 100%;
      padding: {{commentPadding}};
      border: var(--chat-comment-border-style);
      border-radius: {{commentBorderRadius}};
      background-color: var(--chat-comment-rendered-bg) !important;
      color: var(--chat-comment) !important;
      overflow: visible;
      overflow-wrap: anywhere;
      {{autoMarginSide}}: auto;
    }

    span[data-a-target="chat-line-message-body"] a {
      color: var(--chat-comment) !important;
    }

    span[data-a-target="chat-line-message-body"]::before {
      content: {{pointerContent}};
      position: absolute;
      top: 4px;
      {{pointerOuterPosition}}
      width: 21px;
      height: 21px;
      transform: {{pointerOuterTransform}};
      background-color: var(--chat-comment-border);
      border-top-left-radius: 7px;
      border-bottom-right-radius: 6px;
      z-index: -1;
    }

    span[data-a-target="chat-line-message-body"]::after {
      content: {{pointerInnerContent}};
      position: absolute;
      top: 7px;
      {{pointerInnerPosition}}
      width: 18px;
      height: 18px;
      transform: {{pointerInnerTransform}};
      background-color: var(--chat-comment-bg);
      border-top-left-radius: 4px;
      border-bottom-right-radius: 20px;
      z-index: 0;
    }

    .chat-line__message--emote {
      margin: 0 !important;
    }
